(* This program performs a depth-first-search of an adjacency list starting 
  from the head of the adjacency list, i.e. node 0*)
let rec list_nth = fun lst n ->
  if is_nil lst then 
    ()
  else if n = 0 then 
    hd lst 
  else 
    list_nth lst (n - 1)
in 
